spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  init:
    enabled: true
  jwt:
    issuer: test-issuer
    active-secret: test-secret-test-secret-test-secret-test-secret
    previous-secret:
    access-expiry-minutes: 30
    refresh-expiry-days: 30
