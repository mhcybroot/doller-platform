version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: doller
      POSTGRES_USER: doller
      POSTGRES_PASSWORD: doller
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
